{
  "name": "project",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:generate": "npm run db:generate --prefix server",
    "db:studio": "npm run db:studio --prefix server",
    "lint": "concurrently -n client,server \"npm run lint --prefix client\" \"npm run lint --prefix server\"",
    "format": "concurrently -n client,server \"npm run format --prefix client\" \"npm run format --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
